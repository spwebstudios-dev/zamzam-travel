// Homepage placeholder — will be replaced with the full hero/featured-packages build
export default function HomePage() {
  return (
    <section className="flex items-center justify-center min-h-[60vh] px-5">
      <h1
        className="font-serif text-4xl md:text-5xl text-center"
        style={{ color: 'var(--color-primary)' }}
      >
        Homepage
      </h1>
    </section>
  );
}
